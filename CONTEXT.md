# 声音转换

将源音频的声音转换为参考音频的音色，同时保留源音频的内容。

## 术语

**声音转换模型组**：
共同完成声音转换所需的一组模型，包括转换主模型、语义编码器、语者表示模型、声码器和音高提取器。
_避免使用_：单个模型（指代完整模型组时）

**语义编码器**：
从源音频中提取语音内容表征的模型。
_避免使用_：文本分词器（指代音频内容表征模型时）

**声码器**：
将声音转换产生的声学特征还原为音频波形的模型。

**音高提取器**：
从音频中估算基频曲线、为音高调整提供依据的模型。
